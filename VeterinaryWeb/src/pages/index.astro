---
import "@/assets/globals.css";
import Container from "@/schemas/Container.astro";
import CardRating from "@/components/CardRating.vue";
import { Icon } from "@iconify/vue";
import { Separator } from "@/components/ui/separator";
import Badge from "@/components/ui/badge/Badge.vue";
import SectionContact from "@/components/SectionContact.vue";

---

<Container lang="es" title="Home page">
  <meta slot="meta-tags" charset="UTF-8" />
  <meta
    slot="meta-tags"
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />
  <meta
    slot="meta-tags"
    name="description"
    content="soy la web X del X user X"
  />

  <section
    slot="Body"
    class="w-full h-screen flex justify-between items-center px-6 gap-4 sm:flex-col bg-red-500 md:bg-blue-400"
  >
    <div class="w-full px-6 flex justify-center items-center">
      aqui creo que va ser solo como una texto centrado y un contenido, imagen
      de fondo y un boton para contactar o reservar cita...ya vere.
    </div>
  </section>

  <br slot="Body" />1
  <br slot="Body" />

  <section
    class="w-full h-full py-12 flex justify-between items-center px-10"
    slot="Body"
  >
    <div class="w-2/4 flex flex-col gap-4">
      <h1 class="py-3">
        <Badge client:load class="text-sm px-2">
          <div class="flex items-center gap-2">
            <Icon client:load icon="mdi:world" width="16" /> <span> Contact us </span>
          </div>
        </Badge>
      </h1>

      <div class="flex justify-start items-center gap-6 px-2">
        <a href="" class="flex items-center gap-2 text-sm hover:underline">
          <Icon client:load icon="ic:baseline-whatsapp" width="24" />
          <b> Whatsapp </b>
        </a>

        <a href="" class="flex items-center gap-2 text-sm hover:underline">
          <Icon client:load icon="mdi:facebook-messenger" width="24" />
          <b> Messenger </b>
        </a>

        <a href="" class="flex items-center gap-2 text-sm hover:underline">
          <Icon client:load icon="ic:baseline-telegram" width="24" />
          <b> Telegram </b>
        </a>
      </div>

      <Separator label="Or" />

      <div class="form-content flex justify-center items-center">

        <SectionContact client:load></SectionContact>
      
      </div>
    </div>

    <div class="flex justify-center items-center w-2/4">
      
      <!-- Aqui vamos a animar un svg donde se rellene el formulario, nada del otro mundo -->
      <img src="/form-min.svg" width="400" alt="">

    </div>
  </section>

  <section
    slot="Body"
    class="w-full h-screen flex justify-between items-center px-6 gap-4"
  >
    <div class="w-full px-6 flex justify-center items-start flex-col">
      <h1 class="text-4xl font-bold px-2 flex justify-center items-center">
        <Icon client:load icon="line-md:person-search" width="48" /> Customer feedback
      </h1>

      <br />

      <CardRating client:only="vue" />
    </div>

    <div class="flex justify-center items-center p-12">
      <img src="/feedback-2-min.svg" alt="" />
    </div>
  </section>
</Container>
